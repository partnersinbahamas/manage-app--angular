// import { getMaxId } from "src/helpers/functions";
// import { Day } from "../types/day";

export class Day {
  todos = [];

  constructor(
    public date: string,
    public id: number,
    public weekId: number,
  ) {
    // this.id = id;
    // this.date = date,
    // this.weekId = weekId
  }
}